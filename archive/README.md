# Historical archive

`archive/` 只保存历史工作包、评估运行、审查记录和当时的契约证据。当前
导入的 005 工作包位于 [`005-work-package/`](005-work-package/)。

这里的内容用于追溯、比较和审计，不是新 feature 的默认目录，也不是可安装
的 preset、workflow、Skill 或 Extension。新的 Spec Kit feature 仍然必须创建
在根级 [`../specs/`](../specs/) 下。
