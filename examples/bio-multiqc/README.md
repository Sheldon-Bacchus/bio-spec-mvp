# bio-multiqc example

这是 `bio-multiqc` 独立 Extension 的具体示例输入。fixture 位于
[`fixtures/`](fixtures/)，可选 Python 依赖位于
[`requirements.txt`](requirements.txt)。

示例路径只服务于这个 Extension 的显式运行，不是 `control/` 下 generic
workflow 的输入，也不代表任何科学结论或发布批准。
