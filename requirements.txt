# Optional external dependency for the independently scoped bio-multiqc Extension.
# The generic research-control workflow does not execute MultiQC.
# Spec Kit CLI itself is installed separately; see README.md.
multiqc>=1.30
